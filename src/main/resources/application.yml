spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/auth3
    username: root
    password: Intel@123
    initialization-mode: always
security:
  oauth2:
    resource:
      filter-order: 3
  signing-key: <signing-key>
